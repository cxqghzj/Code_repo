#include <iostream>
#include <algorithm>
#include <cstdio>
using namespace std;

int main() {
	freopen("ak.in", "r", stdin);
	freopen("ak.out", "w", stdout);
	int n, k;
	cin >> n >> k;
	for (int i = 1; i <= n; i++) 
		cin >> s[i];

	return 0;
}
